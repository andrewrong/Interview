#include <iostream>
#include <cstring>
#include <fstream>
#include <cassert>

using namespace std;

bool IsMatch(const char* str,const char* re)
{
    assert(str != NULL && re != NULL);

    int strSize = strlen(str);
    int reSize = strlen(re);

}

int main()
{
}
